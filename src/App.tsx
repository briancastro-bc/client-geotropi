import {
  FC,
} from 'react'

import RouterProvider from '@Shared/Providers/RouterProvider';

type AppProps = object;

const App: FC<AppProps> = () => {
  return (
    <RouterProvider/>
  );
};

export default App
