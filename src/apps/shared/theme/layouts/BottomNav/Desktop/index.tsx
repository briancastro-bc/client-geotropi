import { FC, } from 'react';

import {
    motion,
} from 'theme/components';
import { BottomNavProps, } from 'theme/layouts/BottomNav';

type BottomNavDesktopProps = BottomNavProps;

const BottomNavDesktop: FC<BottomNavDesktopProps> = () => {
    return (
        <motion.nav>

        </motion.nav>
    );
};

export default BottomNavDesktop;