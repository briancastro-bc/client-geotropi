import { FC, } from 'react';

type LandingDesktopProps = object;

const LandingDesktop: FC<LandingDesktopProps> = () => {
    return (
        <p>Desktop works!</p>
    );
};

export default LandingDesktop;